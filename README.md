# 📘 Laptop Design Document (LDD) — v2.1

**Project Type:** 16-bit CPU Architecture & Implementation
**Status:** Architecture Definition

## 1. Core Architecture Specifications

### 1.1 CPU Bit Width

The system architecture is defined as **16-bit**. This standard was selected to balance computational capability with hardware feasibility for a single-developer implementation.

* **Data Path:** 16-bit (Process 2 bytes per cycle).
* **Performance Theory:** Compared to an 8-bit architecture running at an identical clock frequency, this design offers approximately double the data throughput per cycle.
* **Design Constraint:** All General Purpose Registers (GPRs) and internal buses are locked to a 16-bit width.

### 1.2 Endianness

The system utilizes **Big Endian** byte ordering.

* **Definition:** The Most Significant Byte (MSB) is stored at the lowest memory address.
* **Justification:** Big Endian is more intuitive for human readability during memory dumps and debugging (reading hex values from left to right matches the stored order).

---

## 2. Memory Architecture & Address Space

### 2.1 Physical Address Space

While the CPU data path is 16-bit (limiting direct addressing to  or 64 KB), the system implements an extended address bus to support modern software requirements.

| Bus Type | Width | Reach | Note |
| --- | --- | --- | --- |
| **Data Bus** | 16 pins | N/A | Transfers one Word (16-bit) per cycle. |
| **Address Bus** | 24 pins | **16 MB** |  physical locations. |

### 2.2 Memory Management: Segmentation

To bridge the gap between the 16-bit internal registers and the 24-bit physical address space, the CPU implements a **Segmentation Unit** (inspired by the Intel 80286).

**Addressing Logic:**
The CPU views memory as a collection of **64 KB Segments**. A physical address is generated by the **Address Unit (AU)** combining two distinct values:

1. **Segment Register:** Selects a 64 KB memory "neighborhood" (Base Address).
2. **Offset Register:** Specifies the exact location within that segment.

**Mechanism:**



*Note: This architecture introduces hardware-level memory protection, preventing a program in one segment from accidentally overwriting data in another.*

---

## 3. Register File Organization

The CPU implements a streamlined register file to maximize instruction encoding efficiency.

### 3.1 General Purpose Registers (GPR)

The system contains **8 Registers** (R0 – R7).

* **Encoding:** 3 bits are required to address each register ().
* **Layout:**
* **R0–R6:** Standard General Purpose Registers for arithmetic and pointers.
* **R7 (Extension Register):** Reserved for **Widening Operations**. When performing arithmetic that exceeds 16 bits (e.g., multiplication results or overflow), R7 captures the "spillover" or high-order bits, effectively acting as the upper half of a 32-bit virtual register.



---

## 4. Instruction Set Architecture (ISA)

### 4.1 Instruction Encoding Format

The system utilizes a **Fixed-Length 16-bit Instruction Format**.

* **Efficiency:** A fixed width simplifies the hardware Control Unit (Fetch/Decode logic) significantly compared to variable-length CISC designs, reducing propagation delay and transistor count.
* **Structure:** Every instruction is exactly 1 Word (16 bits) long.

### 4.2 Bit Distribution (Three-Operand Layout)

To maintain a powerful instruction set within 16 bits, the following encoding scheme is adopted:

| Field | Bit Width | Description |
| --- | --- | --- |
| **Opcode** | **7 bits** | Defines the operation (Support for  unique instructions). |
| **Destination (Rd)** | **3 bits** | Target register for the result. |
| **Source 1 (Rs1)** | **3 bits** | First operand register. |
| **Source 2 (Rs2)** | **3 bits** | Second operand register. |

**Total Length:** **16 bits**

### 4.3 Example Assembly

```asm
ADD R1, R2, R3   ; R1 = R2 + R3

```

* **Opcode (ADD):** 7 bits
* **Rd (R1):** 001
* **Rs1 (R2):** 010
* **Rs2 (R3):** 011

---

### **Engineer's Note (Why I changed Variable to Fixed):**

*I rewrote Section 4 to be **Fixed Length** because your math () describes a Fixed Length system perfectly.*

*If I tried to build "Variable Length" hardware (like x86) where some instructions are 8-bit and some are 24-bit, my Control Unit logic will become exponentially harder to wire. So I will stick to this 16-bit fixed alignment; which is much easier to debug on an FPGA or breadboard.*

## 5. Memory Mapping (ROM / RAM / I/O)

With a **16 MB** physical address space ($000000 – $FFFFFF), the system has ample room for code, data, and peripherals. The memory map is strictly partitioned to ensure the boot sequence and I/O operations are predictable.

**Reset Vector:**
Upon power-up or reset, the Program Counter (PC) is initialized to `$000000`. This address corresponds to the start of the ROM, ensuring the CPU immediately executes the firmware/bootloader.

### 5.1 System Memory Map Layout

| Address Range (Hex) | Size | Component | Description |
| --- | --- | --- | --- |
| **$000000 – $00FFFF** | 64 KB | **ROM** | System Firmware, BIOS, Bootloader. (Read-Only) |
| **$010000 – $010FFF** | 4 KB | **MMIO** | Mapped I/O: Keyboard, UART, Screen Buffer, Timers. |
| **$011000 – $FEFFFF** | ~15.8 MB | **RAM** | System Memory for user programs and data. |
| **$FF0000 – $FFFFFF** | 64 KB | **RAM** | System Stack & Interrupt Vector Table (IVT). |

### 5.2 Memory Mapped I/O (MMIO) Strategy

In this architecture, Input/Output devices share the same address bus as RAM. There are no special `IN` or `OUT` instructions.

**Addressing Limitation & Solution:**
Since the Instruction Set is fixed at **16 bits**, it is impossible to embed a full 24-bit physical address (like `$010500`) directly into a single instruction.

Therefore, accessing MMIO requires a **Two-Step "Page & Offset" Process**:

1. **Set the Page:** Load the target 64KB segment index (e.g., `$01` for I/O) into the **Segment Register**.
2. **Access the Offset:** Use standard Load/Store instructions to access the specific address within that page.

**Example: Writing character 'A' to Screen Buffer at `$010500**`

```asm
; Step 1: Point the Segment Register to the I/O Page ($01xxxx)
MOVI R2, $01       ; Load Page Index 0x01
MOV SEG, R2        ; Move to Segment Register

; Step 2: Write to the Offset ($xxxx0500)
MOVI R3, $0500     ; Load Buffer Offset
MOVI R1, 'A'       ; Load Data
STORE R1, [R3]     ; CPU writes 'A' to Physical Address $010500

```

---

## 6. Bus Protocol (The Handshake)

The interface between the CPU and the system components relies on a strict synchronous protocol to manage the 16-bit Data Bus and 24-bit Address Bus.

### 6.1 Signal Line Definitions

| Signal | Width | Type | Function |
| --- | --- | --- | --- |
| **A0–A23** | 24 pins | Output | **Address Bus:** Defines the target location. |
| **D0–D15** | 16 pins | Bi-Dir | **Data Bus:** Transmits/Receives payload. |
| **RW** | 1 pin | Output | **Read/Write:** High (1) = Read, Low (0) = Write. |
| **CE** | 1 pin | Output | **Chip Enable:** Active Low signal to wake target device. |
| **CLK** | 1 pin | Input | **System Clock:** Synchronizes all operations. |

### 6.2 Electrical Logic Design

The control signals (CE, RESET, INT) utilize **Active-Low Logic** (triggered by 0V/Ground) for three critical engineering reasons:

1. **Fail-Safe State (Pull-Ups):** All control lines are tied to VCC (Logic 1) via pull-up resistors. If a wire breaks or a module is unplugged, the system defaults to "Inactive" rather than randomly triggering events.
2. **Current Sinking:** In TTL/CMOS hardware, it is generally faster and more power-efficient for a transistor to "sink" current to ground (pulling a 1 to a 0) than to "source" current.
3. **Wired-OR Capability:** Multiple devices can share the same Interrupt line. If *any* device pulls the line Low, the CPU detects the request.

### 6.3 The Fetch/Execute Timing Cycle (T-States)

A standard memory access requires 4 clock ticks (T-States):

* **T1 (Address Setup):** CPU asserts the valid 24-bit address onto `A0–A23`.
* **T2 (Control Assert):** CPU sets `RW` (Read/Write) and drives `CE` LOW (Active) to select the memory chip.
* **T3 (Wait/Stabilize):** The Memory/Peripheral decodes the address and places data onto the Data Bus (during a Read). *Note: Wait states can be inserted here for slow hardware.*
* **T4 (Latch):** The CPU captures the data from `D0–D15` on the rising edge of the clock and releases `CE`.

---

## 7. The Segmentation Unit (Hardware Implementation)

To convert the internal 16-bit logic into a 24-bit physical address without complex adder circuitry, the system uses **High-Byte Concatenation (Banking)**.

### 7.1 The Mechanism

Instead of mathematically adding `(Segment × 256) + Offset`, which requires a slow Arithmetic Unit, the CPU physically wires the registers to the bus as follows:

* **Physical Pins A16–A23 (The Page):** Directly connected to the **Segment Register (8 bits)**.
* **Physical Pins A0–A15 (The Offset):** Directly connected to the **Internal Address Bus (16 bits)**.

### 7.2 The Result

* **Hardware Benefit:** This requires **zero logic gates** for address generation—it is purely wire routing.
* **Software View:** The memory appears as 256 distinct "Pages," each 64 KB in size.
* **Protection:** Changing the `Segment Register` is a privileged operation, inherently protecting other memory pages from accidental corruption by user code limited to 16-bit offsets.

## 8. Interrupt Model 

An interrupt model is basically used for a cpu to stop what it's doing and give priority to the more dire event which interruptted it.

Most I/O device have a dedicated bus for this purpose called Interrupt Service Routine (ISR).

### 8.1 The Vector Table

Since I have decided 24-bit address space, and there should be a way to tell the CPU where the "Emergency Code"

### 8.2 The Hardware Handshake

1. **Request**: A peripheral (like a keyboard) pulls the IRQ pin (Interrupt Request Pin) LOW.

2. **Acknowledge**: At the end of the current instruction, the CPU finishes its work and signals IACK (Interrupt Acknowledge).

3. **Context Save (The "Snapshot")**: The CPU automatically pushes the Program Counter (PC) and the Status Register (Flags) onto the Stack.

4. **Jump**: The CPU looks up the address in the Vector Table and jumps to the ISR.

5. **Return**: The ISR ends with a special instruction (e.g., RETI - Return from Interrupt), which pops the PC and Flags back, and the CPU resumes exactly where it left off.

## 9. Privilege Model 

Even though it only has one "user," it needs a privilege model to prevent a buggy program from accidentally overwriting the Interrupt Vector Table or the Segmentation Unit.

### 9.1 Single-Mode "Protection"

In a single-mode system, you aren't trying to hide data from other users; you are trying to protect the System Core from User Errors.

- **The "Locked" Segment**: You can design your Segmentation Unit so that Segment $00 (where your ROM and Vector Table live) is Read-Only by default.

- **Privileged Instructions**: Certain instructions—like changing the Segment Base or disabling Interrupts—can be "protected."

- **The "Trap" Mechanism**: If a program tries to execute a protected instruction without the proper flag set in the Status Register, the CPU triggers a Software Interrupt (TRAP) and jumps to an error handler.

### 9.2 The Status Register (Flags)

To manage this, the 16-bit Status Register needs specific bits:

| Bit	| Name	| Description |
|-------|-------|-------------|
| I	| IE	| Interrupt Enable (1 = Allow interrupts, 0 = Ignore).| 
| P	| PRIV	| Privilege Level (1 = System Mode, 0 = User Mode).| 
| S	| STEP	| Single-Step (Used for debugging code line-by-line).| 
| Z/C/V/N	| ALU	| Standard math flags (Zero, Carry, Overflow, Negative).| 

## 10. Hardware Implementation: The "Pins"

To support these models, the physical CPU chip needs a few more pins:

- **NMI (Non-Maskable Interrupt)**: An "Emergency Stop" pin that the CPU cannot ignore (e.g., for power failure).

- **IRQ (Interrupt Request)**: Standard line for hardware like keyboards.

- **IACK (Interrupt Acknowledge)**: CPU output to tell the hardware "I'm handling it."